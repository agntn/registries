interface:
  display_name: registries AI Tool
  short_description: Integrate @agntn/registries packageTool into Vercel AI SDK apps for structured registry queries
  brand_color: "#3b82f6"
  default_prompt: Set up packageTool for an AI agent that needs to query package registries

policy:
  allow_implicit_invocation: false

dependencies:
  tools:
    - registries
